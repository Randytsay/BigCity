function svgLine(el,series,labels){const w=800,h=250,p=30;let all=series.flatMap(s=>s.values);let min=Math.min(...all)*.96,max=Math.max(...all)*1.04;let colors=['#62a9ff','#37c6a7','#a78bfa','#f2bd5b','#f17f7f'];let path='';series.forEach((s,si)=>{let pts=s.values.map((v,i)=>{let x=p+(w-2*p)*(i/(s.values.length-1));let y=h-p-(h-2*p)*((v-min)/(max-min||1));return [x,y]}).map(a=>a.join(',')).join(' ');path+=`<polyline points="${pts}" fill="none" stroke="${colors[si%colors.length]}" stroke-width="3"/>`;});let grid=[0,1,2,3,4].map(i=>`<line x1="${p}" x2="${w-p}" y1="${p+i*(h-2*p)/4}" y2="${p+i*(h-2*p)/4}" stroke="#1d334b"/>`).join('');el.innerHTML=`<svg viewBox="0 0 ${w} ${h}" preserveAspectRatio="none">${grid}${path}</svg>`}
function svgBars(el,rows){const w=800,h=250,p=32;let max=Math.max(...rows.flatMap(r=>r.values))*1.1;let group=(w-2*p)/rows.length;let colors=['#62a9ff','#37c6a7','#a78bfa','#f2bd5b','#f17f7f'];let out='';rows.forEach((r,i)=>r.values.forEach((v,j)=>{let bw=group/(r.values.length+1);let x=p+i*group+(j+.5)*bw;let bh=(h-2*p)*(v/max);out+=`<rect x="${x}" y="${h-p-bh}" width="${bw*.72}" height="${bh}" rx="4" fill="${colors[j%colors.length]}"/>`}));el.innerHTML=`<svg viewBox="0 0 ${w} ${h}" preserveAspectRatio="none">${[0,1,2,3,4].map(i=>`<line x1="${p}" x2="${w-p}" y1="${p+i*(h-2*p)/4}" y2="${p+i*(h-2*p)/4}" stroke="#1d334b"/>`).join('')}${out}</svg>`}
function fmt(n,d=0){return Number(n).toLocaleString('zh-TW',{maximumFractionDigits:d,minimumFractionDigits:d})}
